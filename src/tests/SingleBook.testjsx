// import { render, screen, fireEvent } from "@testing-library/react";
// import SingleBook from "../components/SingleBook";

// import { describe, it, expect } from "vitest";

// describe("changes border color", () => {
//   it("checks if the card is mounted", () => {
//     render(<SingleBook />);
//     const card = screen.getAllByAltText(book.img);
//     expect(card).toHaveStyle("border: 1px solid gray");
//   });
//   // it("checks if changes color on click", () => {
//   //   render(<SingleBook />);
//   //   const img = screen.getByRole(img);
//   //   fireEvent.click(img);
//   //   expect(card).toHaveStyle("border: 2px solid red");
//   // });
// });
